version: '3.8'

services:
  flask-calculator:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    container_name: flask-calculator-app
